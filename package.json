{
  "name": "little-virtual-computer",
  "version": "0.0.1",
  "description": "Learn how computers work by simulating them in Javascript",
  "scripts": {
    "build": "bun run build-computer & bun run build-simulator",
    "build-simulator": "esbuild src/simulator.ts --bundle --minify --format=esm --outfile=dist/simulator.module.js",
    "build-computer": "esbuild src/index.ts --bundle --minify --format=esm --outfile=dist/computer.module.js"
  },
  "devDependencies": {
    "esbuild": "0.28.0"
  }
}
